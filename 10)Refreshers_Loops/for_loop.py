friends = ["Rohan", "Sourav", "Dip", "Ishan", "Anny"]

for friend in friends:
    print(f"{friend} is my buddy.")
    # this means for each element in a list/set/tuple, here friend a given name of each element
    # anything can be a name of element not particularly friend, we can name elements anything we want,
    # f, frnd, a-z anything
    # for each element in the list/set/tuple the for loop runs the code which is inside the for loop statement
